$cover = /tmp/hyde-mpris.png
$wall = /home/engon/.config/hyprlock/wallpapers/4.jpg

general {
    immediate_render = true
    no_fade_in = true
    no_fade_out = true
    disable_loading_bar = false
    hide_cursor = false
    transparent = true
}


# BACKGROUND
background {
    path = $wall
    # color = rgba(255, 0, 0, 0)
    blur_passes = 0
}



# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I")</span>"
    color =rgb(240, 231, 219)
    font_size = 60
    font_family = TeachingPrintDotted-OVKlp
    position = -60, 200
    halign = center
    valign = center
}

# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +":%M")</span>"
    color = rgb(214, 209, 209)
    font_size = 60
    font_family = TeachingPrintDotted-OVKlp
    position = 45, 200
    halign = center
    valign = center
}

# Day-Month-Date
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%A, %B %d")"
    color = rgb(200, 200, 200)
    font_size = 19
    font_family = TeachingPrintDotted-OVKlp
    position = 0, 340
    halign = center
    valign = center
}



# Profie-Photo
image {
    monitor =
    path = ~/.config/hyprlock/wallpapers/me.jpeg
    border_size = 2
    # border_color = rgb(45, 48, 50)
    size = 190
    rounding = -1
    rotate = 0
    reload_time = -1
    reload_cmd =
    position = 0, 0
    halign = center
    valign = center
}



# USER-BOX
shape {
    monitor =
    size = 200, 50
    color =rgb(0, 0, 0)
    rounding = 15
    border_size = 0
    border_color = rgba(255, 255, 255, 1)
    rotate = 0
    xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)
    position = 0, -335
    halign = center
    valign = center
}

# USER
label {
    monitor =
    # text = <i> Hi! $USER ;) </i>
    text = cmd[update:60000] echo "<i> Good $(H=$(date +%H); if [ $H -lt 12 ]; then echo Morning; elif [ $H -lt 18 ]; then echo Afternoon; else echo Evening; fi) $(echo "${USER:0:1}" | tr '[:lower:]' '[:upper:]')$(echo "${USER:1}" | tr '[:upper:]' '[:lower:]') </i>"
    color = rgba(216, 222, 233, 0.80)
    outline_thickness = 0
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    font_size = 11
    position = 0, -335
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 200, 50
    outline_thickness = 0
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(255, 255, 255, 0)
    inner_color =rgb(0, 0, 0)
    font_color = rgb(200, 200, 200)
    fade_on_empty = true
    placeholder_text = <i><span foreground="##ffffff99">   Enter passwd</span></i>
    hide_input = false
    position = 0, -335
    halign = center
    valign = center
    rounding = 15
}



# Music blur div
shape {
    monitor =
    size = 340, 140
    color =rgb(44, 37, 37)
    rounding = 30
    rotate = 0
    position = -30, -220
    halign = right
    valign = top
    zindex = 1
}



# Music album art
image {
    monitor =
    path = $cover
    size = 110
    rounding = 20
    rotate = 0
    reload_time = 0
    reload_cmd =
    position = -40, -230
    halign = right
    valign = top
    zindex = 5
}


# PLAYER SOURCE
label {
    monitor =
    text = cmd[update:1000] echo "$($music --source)"
    color = rgba(255, 255, 255, 0.6)
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono
    position = -165, -235
    halign = right
    valign = top
    zindex = 5
}


# PLAYER TITLE
label {
    monitor =
    text = cmd[update:1000] echo "$($music --title)"
    color = rgba(255, 255, 255, 0.8)
    font_size = 12
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -165, -260
    halign = right
    valign = top
    zindex = 5
}



# PLAYER ARTIST
label {
    monitor =
    text = cmd[update:1000] echo "$($music --artist)"
    color = rgba(255, 255, 255, 0.8)
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -165, -280
    halign = right
    valign = top
    zindex = 5
}

# NAME
label {
    monitor =
    text = ⟲
    onclick = playerctl -p spotify_player previous
    color = rgb(216, 222, 233)
    font_size = 25
    position = -320, -320
    halign = right
    valign = top
    zindex = 5
}

# PLAYER STATUS
label {
    monitor =
    text = cmd[update:1000] echo "$($music --status)"
    onclick = playerctl -p spotify_player play-pause

    color = rgba(255, 255, 255, 1)
    font_size = 21
    font_family = JetBrains Mono Nerd Font Mono
    position = -265, -320
    halign = right
    valign = top
    zindex = 5
}

# NAME
label {
    monitor =
    text = »
    onclick = playerctl -p spotify_player next

    color = rgb(216, 222, 233)
    font_size = 25
    position = -210, -315
    halign = right
    valign = top
    zindex = 5
}



# Battery div
shape {
    monitor =
    size = 140, 68
    color =rgb(173, 48, 49)
    # color =rgb(240, 170, 170)
    rounding = 20
    rotate = 0
    position = -40, -120
    halign = right
    valign = top
    zindex = 1
}

# Battery
label {
    monitor =
    text = cmd[update:1000] echo -e "$($battery)"
    color = rgb(255, 255, 255)
    font_size = 25
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -60, -135
    halign = right
    valign = top
    zindex = 5
}

# Weather div
shape {
    monitor =
    size = 135, 75
    color = rgb(240, 231, 219)
    # color =rgb(246, 173, 85)
    rounding = 20
    rotate = 0
    position = -200, -120
    halign = right
    valign = top
    xray = false
    zindex = 1
}


# WEATHER
label {
    monitor =
    text = cmd[update:1000] echo "$($weather)"
    color = rgb(45, 48, 50)
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -220, -130
    halign = right
    valign = top
    zindex = 5
}

# LOCATION
label {
    monitor =
    text = cmd[update:1000] echo "$($location)"
    color = rgb(45, 48, 50)
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -230, -160
    halign = right
    valign = top
    zindex = 5
}
